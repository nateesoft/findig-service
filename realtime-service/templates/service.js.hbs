const { mappingResultData } = require("../utils/ConvertThai")

const getData = async ({ repository, db }) => {
  const results = await repository.getData({ payload: {}, db })
  return mappingResultData(results)
};

module.exports = {
  getData
}