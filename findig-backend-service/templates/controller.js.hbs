const { {{name}}Service } = require('../services/{{name}}Service')
const { {{name}}Repository } = require('../services/{{name}}Repository')

const getData = async (req, res, next) => {
  try {
    const result = await {{name}}Service.getData({
      repository: StcardRepository,
      db: req.db
    })
    res.json(result);
  } catch (err) {
    next(err);
  }
};
module.exports = {
  getData
}
