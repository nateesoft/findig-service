const { {{name}}Service } = require('../services/{{name}}Service')
const { {{name}}Repository } = require('../services/{{name}}Repository')

{{#each methods}}
const {{this.controllerName}} = async (req, res, next) => {
  try {
    const result = await {{name}}Service.{{this.serviceName}}({
      repository: {{name}}Repository,
      db: req.db
    })
    res.json(result);
  } catch (err) {
    next(err);
  }
};
{{/each}}

module.exports = {
  {{#each methods}}
    {{this.controllerName}},
  {{/each}}
}
