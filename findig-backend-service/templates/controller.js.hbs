const { {{name}}Service } = require('../services/{{name}}Service.js')

const {{name}}Controller = async (req, res, next) => {
  try {
    const result = await {{name}}Service(req.body);
    res.json(result);
  } catch (err) {
    next(err);
  }
};

{{#each methods}}
const {{name}}Controller = async (req, res, next) => {
  try {
    const result = await {{this.serviceName}}(req.{{this.inputType}});
    res.json(result);
  } catch (err) {
    next(err);
  }
};
{{/each}}

module.exports = {
{{#each methods}}
  {{name}}Controller,
{{/each}}
};
