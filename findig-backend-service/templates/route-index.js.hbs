const express = require('express')
const router = express.Router();

{{#each routes}}
const { {{this.name}}Controller } = require('../controllers/{{this.name}}Controller')
{{/each}}


{{#each routes}}
router.{{this.method}}('{{this.path}}', {{this.name}}.getData);
{{/each}}

module.exports = router
