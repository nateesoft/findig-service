const express = require('express')
const router = express.Router();

{{#each controllers}}
const {
{{#each this.methods}}
  {{this.controllerName}},
{{/each}}
} = require('../controllers/{{this.name}}Controller');
{{/each}}

{{#each controllers}}
{{#each this.methods}}
router.{{this.method}}('{{this.path}}', {{this.controllerName}});
{{/each}}
{{/each}}

module.exports = router
